extends layout

block content
  header(class="hero" style="background-image: url(images/books.webp); height: 400px;")
    div(class="hero-overlay bg-opacity-60")
    div(class="hero-content text-white text-center")
      div(class="max-w-md")
        h1(class="mb-5 text-4xl md:text-5xl lg:text-6xl font-bold")= title
        p(class="mb-5 text-xl md:text-2xl font-semibold") Welcome to #[em Express Library], your local hub for knowledge, learning, and community connection.
        
  div(class="py-5 space-y-2")
    h2(class="text-3xl font-semibold") Record Counts

    p(class="text-lg") The library has the following record counts:

    ul(class="space-y-3")
      li(class="") #[strong Books:] !{book_count}
      li(class="") #[strong Copies:] !{book_instance_count}
      li(class="") #[strong Copies available:] !{book_instance_available_count}
      li(class="") #[strong Authors:] !{author_count}
      li(class="") #[strong Genres:] !{genre_count}
