extends layout

block content
  h1(class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl mb-5 text-balance")= message
  h2(class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl mb-5 text-primary font-bold")= error.status
  pre(class="text-balance") #{error.stack}
